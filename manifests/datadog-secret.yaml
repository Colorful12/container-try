apiVersion: v1
kind: Secret
metadata:
  name: datadog-secret
  namespace: taki
type: Opaque
data:
  # 注意: 実際のAPIキーをbase64エンコードして設定
  # echo -n "your_actual_api_key" | base64
  api-key: <base64-encoded-datadog-api-key> 
